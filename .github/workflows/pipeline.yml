name: pipeline

on:
  push:
    branches:
      - main

jobs:
  echo_branch:
    runs-on: ubuntu-latest
    
    steps:       
      - name: First pipe
        run: "echo Branch: main"
  
  test:
    runs-on: ubuntu-latest

    steps:
      - name: install npm
        run: "npm install"
      - name: run user_test
        run: "npm run test"